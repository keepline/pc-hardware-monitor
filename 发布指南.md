# 🚀 发布指南 - 分享给小伙伴

完整的项目发布流程，包括打包、上传和分享。

## 📋 快速发布流程

### 第一步：打包应用

详细打包步骤请参考 [BUILDING.md](./BUILDING.md)

**快速方法：**
1. 右键 `build-admin.bat` → 以管理员身份运行
2. 等待完成，文件在 `dist` 目录

### 第二步：提交代码到 GitHub

```bash
# 添加所有更改
git add .

# 提交更改
git commit -m "更新版本 v1.0.0"

# 推送到远程仓库
git push origin master
```

### 第三步：创建 GitHub Release

1. **访问仓库**
   ```
   https://github.com/keepline/pc-hardware-monitor
   ```

2. **创建 Release**
   - 点击右侧 **"Releases"** → **"Create a new release"**
   - Tag version: `v1.0.0`
   - Release title: `PC硬件监控工具 v1.0.0`

3. **填写更新说明**（示例）：
   ```markdown
   ## ✨ 首次发布！
   
   ### 功能特性
   - ✅ 实时监控CPU、内存、GPU、磁盘、网络等硬件信息
   - ✅ 黑色科技风格界面
   - ✅ 自动刷新数据（每5秒）
   - ✅ 智能导航标签
   
   ### 下载方式
   - **便携版**（推荐）：下载 `PC-Hardware-Monitor-1.0.0-portable.exe` 双击直接运行
   
   ### 系统要求
   - Windows 10/11（64位）
   
   ---
   **首次发布，欢迎反馈！**
   ```

4. **上传文件**
   - 将 `dist` 目录下的 `.exe` 文件拖拽到上传区域
   - 等待上传完成

5. **发布**
   - 点击 **"Publish release"**

### 第四步：分享给小伙伴

发布完成后，将以下链接分享给小伙伴：

#### 🔗 分享链接

**仓库主页（推荐）：**
```
https://github.com/keepline/pc-hardware-monitor
```

**下载页面（最直接）：**
```
https://github.com/keepline/pc-hardware-monitor/releases
```

**直接下载链接：**
```
https://github.com/keepline/pc-hardware-monitor/releases/download/v1.0.0/PC-Hardware-Monitor-1.0.0-portable.exe
```

---

## 📱 分享话术示例

### 微信/QQ 分享模板

```
分享给你一个我做的PC硬件监控小工具 🖥️

✨ 功能：
- 实时查看CPU、内存、GPU等硬件信息
- 黑色科技风格界面
- 自动刷新，数据实时更新

📥 下载：
https://github.com/keepline/pc-hardware-monitor/releases

点击下载便携版.exe文件，双击直接运行，无需安装！

支持 Windows 10/11 😊
```

---

## 💡 小伙伴使用步骤

告诉你的小伙伴：

1. 打开链接：https://github.com/keepline/pc-hardware-monitor/releases
2. 点击下载：`PC-Hardware-Monitor-1.0.0-portable.exe`
3. 双击运行：无需安装，直接使用！

**注意：** 如果 Windows 提示"未知发布者"，点击"仍要运行"即可。

---

## 🔄 后续版本更新

当你更新代码后：

```bash
# 1. 更新版本号（在 package.json 中）
# 例如：1.0.0 → 1.1.0

# 2. 重新打包
右键 build-admin.bat → 以管理员身份运行

# 3. 提交代码
git add .
git commit -m "版本更新：v1.1.0 - 添加XXX功能"
git push

# 4. 创建新的 Release，上传新的 .exe 文件
```

---

## 🤖 自动化打包（GitHub Actions）

如果想要推送代码后自动打包，创建文件 `.github/workflows/build.yml`：

```yaml
name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

**使用方法：**
```bash
# 创建并推送 tag
git tag v1.0.0
git push origin v1.0.0

# GitHub 会自动打包并创建 Release
```

---

## ⚠️ 注意事项

1. **文件大小**：打包后约 150MB（包含完整运行环境）
2. **安全警告**：Windows 可能提示"未知发布者"，这是正常的
3. **代码签名**：如需去除警告，可购买代码签名证书（约 500-2000 元/年）
4. **版本号**：建议遵循语义化版本规范（如 1.0.0, 1.1.0, 2.0.0）

---

## 📚 相关文档

- [BUILDING.md](./BUILDING.md) - 详细的打包指南
- [README.md](./README.md) - 项目说明文档

---

## 🎉 完成！

现在你的小伙伴就可以通过 GitHub 链接下载和使用你的硬件监控工具了！

有问题？在 [GitHub Issues](https://github.com/keepline/pc-hardware-monitor/issues) 中反馈。
